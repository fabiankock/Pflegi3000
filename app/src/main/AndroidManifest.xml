<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="comfabiankockpflegi3000.github.pflegi3000">

    <application
        android:name=".database.DaoFactory"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.main_activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.add_patient_activity.AddPatientActivity" />
        <activity android:name=".activities.search_patient_activity.SearchPatientActivity" />
        <activity
            android:name=".activities.show_patient_activity.ShowPatientActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".activities.alter_tables_activity.AlterTablesActivity"></activity>
    </application>

</manifest>